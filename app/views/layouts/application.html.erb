<!DOCTYPE html>
<html>
<head>
  <title>SportFoot – інтернет-магазин спортивного взуття</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap 5 CDN – працює завжди і всюди -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
</head>

<body class="bg-light">
<nav class="navbar navbar-dark bg-dark mb-4">
  <div class="container-fluid">
    <a class="navbar-brand fs-3" href="/">SportFoot</a>
    <div>
      <% if user_signed_in? %>
        <span class="text-white me-3">Привіт, <%= current_user.email %></span>
        <%= link_to "Вийти", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-light" %>
      <% else %>
        <%= link_to "Увійти", new_user_session_path, class: "btn btn-outline-light me-2" %>
        <%= link_to "Реєстрація", new_user_registration_path, class: "btn btn-light" %>
      <% end %>
    </div>
  </div>
</nav>

<div class="container">
  <% if notice %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= notice %>
      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
  <% end %>

  <%= yield %>
</div>

<!-- Bootstrap JS CDN -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>